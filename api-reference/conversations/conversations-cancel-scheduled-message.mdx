---
title: 'Cancel a scheduled message.'
description: 'Post the messageId for the API to delete a scheduled message. <br />'
---

Post the messageId for the API to delete a scheduled message. &lt;br /&gt;

```http
DELETE https://services.leadconnectorhq.com/conversations/messages/{messageId}/schedule
```

## Autorizacion

<ParamField header="Authorization" type="string" required>
Bearer token generado desde el portal Leadway. Ver [Autenticacion](/authentication).
</ParamField>

<ParamField header="Version" type="string" required default="2021-07-28">
Version de la API.
</ParamField>

## Path parameters

<ParamField path="messageId" type="string" required default='ve9EPM428h8vShlRW1KT'>
Message Id
</ParamField>

## Respuestas

<Accordion title="200 - The scheduled message was cancelled successfully">
<ResponseField name="status" type="number" required default='404'>
HTTP Status code of the request
</ResponseField>

<ResponseField name="message" type="string" required default='Failed cancel the scheduled message'>
Error message of the request
</ResponseField>

```json
{
  "status": 404,
  "message": "Failed cancel the scheduled message"
}
```
</Accordion>

<Accordion title="400 - Bad Request">
```json
{}
```
</Accordion>

<Accordion title="401 - Unauthorized">
```json
{}
```
</Accordion>


## Ejemplo

<CodeGroup>

```bash cURL
curl -X DELETE 'https://services.leadconnectorhq.com/conversations/messages/YOUR_messageId/schedule' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -H 'Version: 2021-07-28'
```

```javascript Node.js
const response = await fetch('https://services.leadconnectorhq.com/conversations/messages/YOUR_messageId/schedule', {
  method: 'DELETE',
  headers: {
    Authorization: `Bearer ${process.env.LEADWAY_TOKEN}`,
    Version: '2021-07-28',
  },
});
const data = await response.json();
```

```python Python
import os, requests
response = requests.request(
    'DELETE',
    'https://services.leadconnectorhq.com/conversations/messages/YOUR_messageId/schedule',
    headers={
        'Authorization': f"Bearer {os.environ['LEADWAY_TOKEN']}",
        'Version': '2021-07-28',
    },
)
data = response.json()
```

```php PHP
<?php
$ch = curl_init('https://services.leadconnectorhq.com/conversations/messages/YOUR_messageId/schedule');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'DELETE',
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . getenv('LEADWAY_TOKEN'),
        'Version: 2021-07-28',
    ],
]);
$data = json_decode(curl_exec($ch), true);
```

</CodeGroup>
