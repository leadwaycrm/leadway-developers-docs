---
title: 'Get Groups'
description: 'Get all calendar groups in a account.'
---

Get all calendar groups in a account.

```http
GET https://services.leadconnectorhq.com/calendars/groups
```

## Autorización

<ParamField header="Authorization" type="string" required>
Bearer token generado desde el portal Leadway. Ver [Autenticación](/authentication).
</ParamField>

<ParamField header="Version" type="string" required default="2021-07-28">
Versión de la API.
</ParamField>

## Query parameters

<ParamField query="locationId" type="string" required default='ve9EPM428h8vShlRW1KT'>
Account Id
</ParamField>

## Respuestas

<Accordion title="200 - Successful response">
<ResponseField name="groups" type="object[]">
<Expandable title="cada item">
<ResponseField name="locationId" type="string" required default='ocQHyuzHvysMo5N5VsXc'>
</ResponseField>

<ResponseField name="name" type="string" required default='group a'>
</ResponseField>

<ResponseField name="description" type="string" required default='group description'>
</ResponseField>

<ResponseField name="slug" type="string" required default='15-mins'>
</ResponseField>

<ResponseField name="isActive" type="boolean" default='True'>
</ResponseField>

<ResponseField name="id" type="string" default='ocQHyuzHvysMo5N5VsXc'>
</ResponseField>

</Expandable>
</ResponseField>

```json
{
  "groups": [
    {
      "locationId": "ocQHyuzHvysMo5N5VsXc",
      "name": "group a",
      "description": "group description",
      "slug": "15-mins",
      "isActive": true,
      "id": "ocQHyuzHvysMo5N5VsXc"
    }
  ]
}
```
</Accordion>

<Accordion title="400 - Bad Request">
```json
{}
```
</Accordion>

<Accordion title="401 - Unauthorized">
```json
{}
```
</Accordion>


## Ejemplo

<CodeGroup>

```bash cURL
curl -X GET 'https://services.leadconnectorhq.com/calendars/groups' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -H 'Version: 2021-07-28'
```

```javascript Node.js
const response = await fetch('https://services.leadconnectorhq.com/calendars/groups', {
  method: 'GET',
  headers: {
    Authorization: `Bearer ${process.env.LEADWAY_TOKEN}`,
    Version: '2021-07-28',
  },
});
const data = await response.json();
```

```python Python
import os, requests
response = requests.request(
    'GET',
    'https://services.leadconnectorhq.com/calendars/groups',
    headers={
        'Authorization': f"Bearer {os.environ['LEADWAY_TOKEN']}",
        'Version': '2021-07-28',
    },
)
data = response.json()
```

```php PHP
<?php
$ch = curl_init('https://services.leadconnectorhq.com/calendars/groups');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'GET',
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . getenv('LEADWAY_TOKEN'),
        'Version: 2021-07-28',
    ],
]);
$data = json_decode(curl_exec($ch), true);
```

</CodeGroup>
