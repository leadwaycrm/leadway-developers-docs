---
title: 'Update Group'
description: 'Update Group by group ID'
---

Update Group by group ID

```http
PUT https://services.leadconnectorhq.com/calendars/groups/{groupId}
```

## Autorización

<ParamField header="Authorization" type="string" required>
Bearer token generado desde el portal Leadway. Ver [Autenticación](/authentication).
</ParamField>

<ParamField header="Version" type="string" required default="2021-07-28">
Versión de la API.
</ParamField>

## Path parameters

<ParamField path="groupId" type="string" required default='ocQHyuzHvysMo5N5VsXc'>
Group Id
</ParamField>

## Body

<ParamField body="name" type="string" required default='group a'>
</ParamField>

<ParamField body="description" type="string" required default='group description'>
</ParamField>

<ParamField body="slug" type="string" required default='15-mins'>
</ParamField>

## Respuestas

<Accordion title="200 - Successful response">
<ResponseField name="group" type="object">
<Expandable title="propiedades">
<ResponseField name="locationId" type="string" required default='ocQHyuzHvysMo5N5VsXc'>
</ResponseField>

<ResponseField name="name" type="string" required default='group a'>
</ResponseField>

<ResponseField name="description" type="string" required default='group description'>
</ResponseField>

<ResponseField name="slug" type="string" required default='15-mins'>
</ResponseField>

<ResponseField name="isActive" type="boolean" default='True'>
</ResponseField>

<ResponseField name="id" type="string" default='ocQHyuzHvysMo5N5VsXc'>
</ResponseField>

</Expandable>
</ResponseField>

```json
{
  "group": {
    "locationId": "ocQHyuzHvysMo5N5VsXc",
    "name": "group a",
    "description": "group description",
    "slug": "15-mins",
    "isActive": true,
    "id": "ocQHyuzHvysMo5N5VsXc"
  }
}
```
</Accordion>

<Accordion title="400 - Bad Request">
```json
{}
```
</Accordion>

<Accordion title="401 - Unauthorized">
```json
{}
```
</Accordion>


## Ejemplo

<CodeGroup>

```bash cURL
curl -X PUT 'https://services.leadconnectorhq.com/calendars/groups/YOUR_groupId' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -H 'Version: 2021-07-28' \
  -H 'Content-Type: application/json' \
  -d '{
  "name": "group a",
  "description": "group description",
  "slug": "15-mins"
}'
```

```javascript Node.js
const response = await fetch('https://services.leadconnectorhq.com/calendars/groups/YOUR_groupId', {
  method: 'PUT',
  headers: {
    Authorization: `Bearer ${process.env.LEADWAY_TOKEN}`,
    Version: '2021-07-28',
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({"name": "group a", "description": "group description", "slug": "15-mins"}),
});
const data = await response.json();
```

```python Python
import os, requests
response = requests.request(
    'PUT',
    'https://services.leadconnectorhq.com/calendars/groups/YOUR_groupId',
    headers={
        'Authorization': f"Bearer {os.environ['LEADWAY_TOKEN']}",
        'Version': '2021-07-28',
        'Content-Type': 'application/json',
    },
    json={"name": "group a", "description": "group description", "slug": "15-mins"},
)
data = response.json()
```

```php PHP
<?php
$ch = curl_init('https://services.leadconnectorhq.com/calendars/groups/YOUR_groupId');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'PUT',
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . getenv('LEADWAY_TOKEN'),
        'Version: 2021-07-28',
        'Content-Type: application/json',
    ],
    CURLOPT_POSTFIELDS => json_encode({'name': 'group a', 'description': 'group description', 'slug': '15-mins'}),
]);
$data = json_decode(curl_exec($ch), true);
```

</CodeGroup>
