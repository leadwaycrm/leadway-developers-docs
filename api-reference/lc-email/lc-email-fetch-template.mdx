---
title: 'Fetch email templates'
description: 'Fetch email templates by account id'
---

Fetch email templates by account id

```http
GET https://services.leadconnectorhq.com/emails/builder
```

## Autorizacion

<ParamField header="Authorization" type="string" required>
Bearer token generado desde el portal Leadway. Ver [Autenticacion](/authentication).
</ParamField>

<ParamField header="Version" type="string" required default="2021-07-28">
Version de la API.
</ParamField>

## Query parameters

<ParamField query="locationId" type="string" required default='ve9EPM428h8vShlRW1KT'>
</ParamField>

<ParamField query="limit" type="string">
</ParamField>

<ParamField query="offset" type="string">
</ParamField>

<ParamField query="search" type="string">
</ParamField>

<ParamField query="sortByDate" type="string">
</ParamField>

<ParamField query="archived" type="string">
</ParamField>

<ParamField query="builderVersion" type="string">
</ParamField>

<ParamField query="name" type="string">
</ParamField>

<ParamField query="parentId" type="string">
</ParamField>

<ParamField query="originId" type="string" default='ve9EPM428h8vShlRW1KT'>
</ParamField>

<ParamField query="templatesOnly" type="string">
</ParamField>

## Respuestas

<Accordion title="200 - Success">
<ResponseField name="name" type="string" default='New Template'>
template name
</ResponseField>

<ResponseField name="updatedBy" type="string" default='John Doe'>
updated by
</ResponseField>

<ResponseField name="isPlainText" type="boolean" default='false'>
plain text based template
</ResponseField>

<ResponseField name="lastUpdated" type="string" default='2024-11-12T12:34:36.070Z'>
last updated
</ResponseField>

<ResponseField name="dateAdded" type="string" default='2024-11-12T12:34:36.070Z'>
date added
</ResponseField>

<ResponseField name="previewUrl" type="string" default='https://example.com'>
preview url
</ResponseField>

<ResponseField name="id" type="string" default='67334b231f2fad724062f52b5'>
id
</ResponseField>

<ResponseField name="version" type="string" default='1'>
version
</ResponseField>

<ResponseField name="templateType" type="string" default='builder'>
type
</ResponseField>

```json
{
  "name": "New Template",
  "updatedBy": "John Doe",
  "isPlainText": "false",
  "lastUpdated": "2024-11-12T12:34:36.070Z",
  "dateAdded": "2024-11-12T12:34:36.070Z",
  "previewUrl": "https://example.com",
  "id": "67334b231f2fad724062f52b5",
  "version": "1",
  "templateType": "builder"
}
```
</Accordion>

<Accordion title="400 - Bad Request">
```json
{}
```
</Accordion>

<Accordion title="401 - Unauthorized">
```json
{}
```
</Accordion>

<Accordion title="404 - Not Found">
</Accordion>

<Accordion title="422 - Unprocessable Entity">
```json
{}
```
</Accordion>


## Ejemplo

<CodeGroup>

```bash cURL
curl -X GET 'https://services.leadconnectorhq.com/emails/builder' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -H 'Version: 2021-07-28'
```

```javascript Node.js
const response = await fetch('https://services.leadconnectorhq.com/emails/builder', {
  method: 'GET',
  headers: {
    Authorization: `Bearer ${process.env.LEADWAY_TOKEN}`,
    Version: '2021-07-28',
  },
});
const data = await response.json();
```

```python Python
import os, requests
response = requests.request(
    'GET',
    'https://services.leadconnectorhq.com/emails/builder',
    headers={
        'Authorization': f"Bearer {os.environ['LEADWAY_TOKEN']}",
        'Version': '2021-07-28',
    },
)
data = response.json()
```

```php PHP
<?php
$ch = curl_init('https://services.leadconnectorhq.com/emails/builder');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'GET',
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . getenv('LEADWAY_TOKEN'),
        'Version: 2021-07-28',
    ],
]);
$data = json_decode(curl_exec($ch), true);
```

</CodeGroup>
