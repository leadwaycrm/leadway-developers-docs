---
title: 'Get entities'
description: 'Retrieve Facebook campaigns, adsets, or ads based on entity type'
---

Retrieve Facebook campaigns, adsets, or ads based on entity type

```http
GET https://services.leadconnectorhq.com/ad-publishing/facebook/entity
```

## Autorización

<ParamField header="Authorization" type="string" required>
Bearer token generado desde el portal Leadway. Ver [Autenticación](/authentication).
</ParamField>

<ParamField header="Version" type="string" required default="2021-07-28">
Versión de la API.
</ParamField>

## Query parameters

<ParamField query="locationId" type="string" required default='loc_abc123'>
Account identifier
</ParamField>

<ParamField query="type" type="string" required default='AD_MANAGER'>
Integration source type
</ParamField>

<ParamField query="next" type="string" default='cursor_abc'>
Pagination cursor
</ParamField>

<ParamField query="fetchAll" type="string" default='true'>
Fetch all entities
</ParamField>

<ParamField query="campaignId" type="string" default='camp_123'>
Campaign identifier
</ParamField>

<ParamField query="adSetId" type="string" default='adset_123'>
Ad set identifier
</ParamField>

<ParamField query="entityType" type="string" required default='CAMPAIGN'>
Entity type to fetch
</ParamField>

<ParamField query="searchId" type="string" default='search_123'>
Search identifier
</ParamField>

<ParamField query="selectedAdAccountId" type="string" default='act_123456'>
Selected ad account ID
</ParamField>

## Respuestas

<Accordion title="200 - Response">
</Accordion>

<Accordion title="400 - Bad Request">
```json
{}
```
</Accordion>

<Accordion title="401 - Unauthorized">
```json
{}
```
</Accordion>

<Accordion title="422 - Unprocessable Entity">
```json
{}
```
</Accordion>


## Ejemplo

<CodeGroup>

```bash cURL
curl -X GET 'https://services.leadconnectorhq.com/ad-publishing/facebook/entity' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -H 'Version: 2021-07-28'
```

```javascript Node.js
const response = await fetch('https://services.leadconnectorhq.com/ad-publishing/facebook/entity', {
  method: 'GET',
  headers: {
    Authorization: `Bearer ${process.env.LEADWAY_TOKEN}`,
    Version: '2021-07-28',
  },
});
const data = await response.json();
```

```python Python
import os, requests
response = requests.request(
    'GET',
    'https://services.leadconnectorhq.com/ad-publishing/facebook/entity',
    headers={
        'Authorization': f"Bearer {os.environ['LEADWAY_TOKEN']}",
        'Version': '2021-07-28',
    },
)
data = response.json()
```

```php PHP
<?php
$ch = curl_init('https://services.leadconnectorhq.com/ad-publishing/facebook/entity');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'GET',
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . getenv('LEADWAY_TOKEN'),
        'Version: 2021-07-28',
    ],
]);
$data = json_decode(curl_exec($ch), true);
```

</CodeGroup>
