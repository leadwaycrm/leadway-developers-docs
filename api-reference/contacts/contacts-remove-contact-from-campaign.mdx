---
title: 'Remove Contact From Campaign'
description: 'Remove Contact From Campaign'
---

Remove Contact From Campaign

```http
DELETE https://services.leadconnectorhq.com/contacts/{contactId}/campaigns/{campaignId}
```

## Autorización

<ParamField header="Authorization" type="string" required>
Bearer token generado desde el portal Leadway. Ver [Autenticación](/authentication).
</ParamField>

<ParamField header="Version" type="string" required default="2021-07-28">
Versión de la API.
</ParamField>

## Path parameters

<ParamField path="contactId" type="string" required default='3bZD1nQzbul0MCancbQD'>
Contact Id
</ParamField>

<ParamField path="campaignId" type="string" required default='Y2I9XM7aO1hncuSOlc9L'>
Campaigns Id
</ParamField>

## Respuestas

<Accordion title="200 - Successful response">
<ResponseField name="succeded" type="boolean" default='True'>
</ResponseField>

```json
{
  "succeded": true
}
```
</Accordion>

<Accordion title="400 - Bad Request">
```json
{}
```
</Accordion>

<Accordion title="401 - Unauthorized">
```json
{}
```
</Accordion>

<Accordion title="422 - Unprocessable Entity">
```json
{}
```
</Accordion>


## Ejemplo

<CodeGroup>

```bash cURL
curl -X DELETE 'https://services.leadconnectorhq.com/contacts/YOUR_contactId/campaigns/YOUR_campaignId' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -H 'Version: 2021-07-28'
```

```javascript Node.js
const response = await fetch('https://services.leadconnectorhq.com/contacts/YOUR_contactId/campaigns/YOUR_campaignId', {
  method: 'DELETE',
  headers: {
    Authorization: `Bearer ${process.env.LEADWAY_TOKEN}`,
    Version: '2021-07-28',
  },
});
const data = await response.json();
```

```python Python
import os, requests
response = requests.request(
    'DELETE',
    'https://services.leadconnectorhq.com/contacts/YOUR_contactId/campaigns/YOUR_campaignId',
    headers={
        'Authorization': f"Bearer {os.environ['LEADWAY_TOKEN']}",
        'Version': '2021-07-28',
    },
)
data = response.json()
```

```php PHP
<?php
$ch = curl_init('https://services.leadconnectorhq.com/contacts/YOUR_contactId/campaigns/YOUR_campaignId');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'DELETE',
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . getenv('LEADWAY_TOKEN'),
        'Version: 2021-07-28',
    ],
]);
$data = json_decode(curl_exec($ch), true);
```

</CodeGroup>
