---
title: 'Delete shipping rate'
description: 'Delete specific shipping rate with Id :shippingRateId'
---

Delete specific shipping rate with Id :shippingRateId

```http
DELETE https://services.leadconnectorhq.com/store/shipping-zone/{shippingZoneId}/shipping-rate/{shippingRateId}
```

## Autorización

<ParamField header="Authorization" type="string" required>
Bearer token generado desde el portal Leadway. Ver [Autenticación](/authentication).
</ParamField>

<ParamField header="Version" type="string" required default="2021-07-28">
Versión de la API.
</ParamField>

## Path parameters

<ParamField path="shippingZoneId" type="string" required default='6578278e879ad2646715ba9c'>
ID of the shipping zone
</ParamField>

<ParamField path="shippingRateId" type="string" required default='6578278e879ad2646715ba9c'>
ID of the shipping rate that needs to be returned
</ParamField>

## Query parameters

<ParamField query="altId" type="string" required default='6578278e879ad2646715ba9c'>
Account Id or Agency Id
</ParamField>

<ParamField query="altType" type="string" required>
</ParamField>

## Respuestas

<Accordion title="200 - Successful response">
<ResponseField name="status" type="boolean" required default='True'>
Status of api action
</ResponseField>

<ResponseField name="message" type="string" default='Successfully created'>
Success message
</ResponseField>

```json
{
  "status": true,
  "message": "Successfully created"
}
```
</Accordion>

<Accordion title="400 - Bad Request">
```json
{}
```
</Accordion>

<Accordion title="401 - Unauthorized">
```json
{}
```
</Accordion>

<Accordion title="422 - Unprocessable Entity">
```json
{}
```
</Accordion>


## Ejemplo

<CodeGroup>

```bash cURL
curl -X DELETE 'https://services.leadconnectorhq.com/store/shipping-zone/YOUR_shippingZoneId/shipping-rate/YOUR_shippingRateId' \
  -H 'Authorization: Bearer YOUR_TOKEN' \
  -H 'Version: 2021-07-28'
```

```javascript Node.js
const response = await fetch('https://services.leadconnectorhq.com/store/shipping-zone/YOUR_shippingZoneId/shipping-rate/YOUR_shippingRateId', {
  method: 'DELETE',
  headers: {
    Authorization: `Bearer ${process.env.LEADWAY_TOKEN}`,
    Version: '2021-07-28',
  },
});
const data = await response.json();
```

```python Python
import os, requests
response = requests.request(
    'DELETE',
    'https://services.leadconnectorhq.com/store/shipping-zone/YOUR_shippingZoneId/shipping-rate/YOUR_shippingRateId',
    headers={
        'Authorization': f"Bearer {os.environ['LEADWAY_TOKEN']}",
        'Version': '2021-07-28',
    },
)
data = response.json()
```

```php PHP
<?php
$ch = curl_init('https://services.leadconnectorhq.com/store/shipping-zone/YOUR_shippingZoneId/shipping-rate/YOUR_shippingRateId');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'DELETE',
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer ' . getenv('LEADWAY_TOKEN'),
        'Version: 2021-07-28',
    ],
]);
$data = json_decode(curl_exec($ch), true);
```

</CodeGroup>
